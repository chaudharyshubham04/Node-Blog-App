<header class="main-header">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <a class="navbar-brand" href="/">BlogON!</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                  <ul class="navbar-nav">
                    <li class="nav-item">
                      <a class="nav-link" href="/blog/all">Home</a>
                    </li>
                    <% if(isAuthenticated) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/blog/addBlog">Add blog</a>
                        </li>
                        <% if(userId){ %>
                        <li class="nav-item">
                            <a class="nav-link" href="/user/<%= userId._id %>">My Profile</a>
                        </li>
                        <% } %>
                       <% } %>
                       <% if(!isAuthenticated) { %>
                        <li class="nav-item" >
                                <a class="nav-link" href="/login">Login</a>
                            </li>
                            <li class="nav-item" >
                                    <a class="nav-link" href="/signup">Signup</a>
                            </li>
                        
                   <% } else { %>
                        <li class="nav-item" >
                                <form action="/logout" method="post">
                                    <button style="border: none" class="nav-link btn btn-block btn-outline-secondary" type="submit">Logout</button>
                                </form>
                        </li>
                    <% } %>
    

                  </ul>
                </div>
              </nav>
    </header>